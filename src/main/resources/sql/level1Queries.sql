-- Total population (and year) within the data set, as calculated from the database.--
SELECT COUNT(lga_name) from lga;

--Total population (and year) within the data set, as calculated from the database.--
SELECT SUM(NUM_PEOPLE) FROM AGE;

